exercise1_1
aim:
to observse what might change if we miss out few programs while running code for hello world
1. studio.h is removed:op is an eror warning because print f in declared inside header file 
2. when semicolon is not added in statmetnt op is a syntax error
3. when \n is removed the cursor stays on same line
4. when there was a spelling mistake in stdio.h and include printf() was not declared 
